{
  "devDependencies": {
    "csslint": "^1.0.5",
    "jshint": "^2.13.6",
    "prettier": "^3.3.1",
    "turbo": "^2.0.3",
    "wrangler": "^3.60.1"
  },
  "scripts": {
    "alint": "arc lint",
    "dbuild": "cd docusaurus && docusaurus build",
    "dserve": "cd docusaurus && docusaurus serve",
    "dstart": "cd docusaurus && docusaurus start",
    "lighthouse": "lhci autorun",
    "lint:eslint": "cd docusaurus && eslint .",
    "lint:test": "arc lint && prettier --check .",
    "p:c": "prettier --check",
    "p:w": "prettier --write",
    "test": "cd docusaurus && docusaurus build && docusaurus serve"
  },
  "workspaces": [
    "./docusaurus"
  ]
}
