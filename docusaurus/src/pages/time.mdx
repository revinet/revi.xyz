# What time is it in revi's timezone?

import Clock from '@site/src/components/timer'; // Import the Clock component

:::info

This clock works via the client-side JavaScript, and if you've disabled it, it might not work correctly.

:::

<Clock />
